# Runtime dependencies (installed by Vercel's @vercel/python builder).
# Flask is the web layer; keyword extraction / scoring is pure-Python (no NLP libs),
# keeping the serverless bundle small and cold-starts fast.
flask>=3.0
flask-cors>=4.0
# PDF text extraction. Pure-Python (no C extensions) — the lone ingestion dependency;
# docx/pptx/xlsx are read with the stdlib (zipfile + xml.etree), no Office libraries.
pypdf>=4.0
